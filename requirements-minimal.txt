# Minimal requirements - Just what we need to test Phase 0 and 1.1
# These all have pre-built wheels for Python 3.13 on Windows

# Core
requests>=2.31.0
python-dotenv>=1.0.0

# Only if you need data processing (optional for now)
# pandas
# numpy

# Testing utilities
geopy>=2.4.0
python-dateutil>=2.8.2
pytz>=2024.1
